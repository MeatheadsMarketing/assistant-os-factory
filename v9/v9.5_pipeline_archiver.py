# v9.5_pipeline_archiver.py
"""Save all pipeline assets (code, manifest, logs) into a versioned folder."""

import streamlit as st
st.set_page_config(page_title="v9.5_pipeline_archiver.py", layout="wide")
st.title("📤 V9.5 Pipeline Archiver")

st.markdown("🔧 Save all pipeline assets (code, manifest, logs) into a versioned folder.")
